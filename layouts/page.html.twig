{% extends '_default/page.html.twig' %}

{% block head_css %}
    <link rel="stylesheet" href="{{ asset('css/tailwind.css', {minify: true}) }}">
    <link rel="stylesheet" href="{{ asset('css/theme.css', {minify: true}) }}">
    <link rel="stylesheet" href="{{ asset('css/fonts.css', {minify: true}) }}">
{% endblock %}
